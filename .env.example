# Tailscale Authentication Key
# 從 https://login.tailscale.com/admin/settings/keys 獲取
#
# 注意：由於配置使用了標籤 (tag:ssh-gateway)，auth key 必須有標籤權限
# 建議生成 auth key 時選擇：
#   - ✅ Reusable（可重複使用）
#   - ✅ Tags: ssh-gateway（選擇此標籤）
#   - ⚠️  不要選擇 Ephemeral（使用標籤時不能是臨時金鑰）
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
